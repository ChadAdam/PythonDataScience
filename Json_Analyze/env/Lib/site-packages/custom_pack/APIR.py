import json
import requests
class my_class(object):
    def __init__(self , url , index):
        self.url = url
        self.index = index
        self.json = None
        self.arg_dict = dict()
        #for arg in args:
           # arg_dict[arg] = None

    def _true_url(index):
        return url + '/'+ index

    def _find_kv(json, arg_dict):
        for dic in json.json():
            for k,v in dic.items():
                #arg_dict[key]=dic.values
                arg_dict[k] = None
        print(arg_dict)
        return arg_dict
           
    

    def get_json_object():
        # Add Error checker for 200 code 
        json=requests.get(_true_url(index))

    def print_parem(json , arg_dict):
        dic = _find_kv( json , arg_dict)
        for k in dic.keys():
            print(k)



